/*
TODO : SOLVE THAT
*/
#include <stdio.h>
#include <stdlib.h>
int main()
{
    int n, x, cont = 0;
    do
    {
        scanf("%d", &n);
        if (n == 0)
        {
            break;
        }

        for (int i = 0; i < n; i++)
        {
            scanf("%d", &x);
            if (x)
            {
            }
        }
        printf("%d\n", cont);
    } while (n != 0);

    return 0;
}