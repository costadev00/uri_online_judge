/*

*/
#include <bits/stdc++.h>
using namespace std;
int main()
{
    int l, c, cont;
    cin >> l >> c;
    int mat[l][c];
    for (int i = 0; i < l; i++)
    {
        for (int j = 0; j < c; j++)
        {
            cin >> mat[i][j];
        }
    }

        return 0;
}