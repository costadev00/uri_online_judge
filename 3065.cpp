/*
TODO : SOLVE
*/
#include <bits/stdc++.h>
#include <iostream>
using namespace std;
int main()
{
    int m, n, res = 0;
    char exp[100];
    do
    {
        /* code */

        cin >> m;
        for (int i = 0; i < m; i++)
        {
            // cin >> n;
            cin >> exp;
            for (int j = 0; j < strlen(exp); j++)
            {
                if (exp[j] == '+')
                    if (j % 2 == 0)
                    {
                        vet_nums = to_int;
                    }
            }
        }
        while (m != 0)
            ;
        return 0;
    }