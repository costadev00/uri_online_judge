/*
TODO sOLVE
*/
#include <bits/stdc++.h>
using namespace std;
int main()
{
    int a, d, b, t, l;
    float valor_golpe, valor_golpe2;
    valor_golpe = ((a + d) / 2) + b;
    valor_golpe2 = ((a + d) / 2) + b;
    cin >> t;
    for (int i = 0; i < t * 2; i++)
    {
        cin >> b;
        cin >> a >> d >> l;
        if (l % 2 == 0)
        {
            valor_golpe2
        }
    }
    return 0;
}