#include <bits/stdc++.h>

#define fastio                        \
    ios_base::sync_with_stdio(false); \
    cin.tie(NULL)

#define endl "\n"

using namespace std;

int main()
{
    fastio;
    int t, n, k, x;
    float res;
    cin >> t;
    for (int i = 0; i < t; i++)
    {
        cin >> n >> k >> x;
        res =
            printf("A chance de Basy acertar o numero no dia %d eh %.2f%\n", i, res);
    }
    return 0;
}