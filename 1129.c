/*

*/
#include <stdio.h>
#include <stdlib.h>
int main()
{
    int n, a, b, c, d, e;
    do
    {
        scanf("%d", &n);
        if (n == 0)
        {
            break;
        }

        for (int i = 0; i < n; i++)
        {
            scanf("%d %d %d %d %d", &a, &b, &c, &d, &e);
            if (a)
        }
    } while (n != 0);
    return 0;
}